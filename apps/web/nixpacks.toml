# Deploy from repo root with NIXPACKS_CONFIG_FILE=apps/web/nixpacks.toml

[phases.install]
cmds = ["corepack enable", "pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @newsarb/web build"]

[start]
cmd = "cd apps/web && pnpm start"
