# ===========================================
# DATABASE (Required)
# ===========================================
# PostgreSQL connection URL
# Railway provides this automatically as DATABASE_URL
DATABASE_URL="postgresql://user:password@localhost:5432/newsarb?schema=public"

# Redis connection URL
# Railway provides this automatically as REDIS_URL
REDIS_URL="redis://localhost:6379"

# ===========================================
# API SERVER (Required in production)
# ===========================================
# JWT secret for API authentication - MUST be set in production
# Generate with: openssl rand -base64 32
JWT_SECRET="your-jwt-secret-here"

# Frontend URL for CORS
FRONTEND_URL="http://localhost:3000"

# Port (Railway sets this automatically)
PORT="3001"

# ===========================================
# WEB FRONTEND (Required)
# ===========================================
# NextAuth.js secret - MUST be set in production
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here"

# Full URL of the frontend (for NextAuth callbacks)
NEXTAUTH_URL="http://localhost:3000"

# API URL (backend server)
# For Railway: use internal networking URL or public API URL
API_URL="http://localhost:3001"

# Public API URL for browser requests
NEXT_PUBLIC_API_URL="http://localhost:3001"

# ===========================================
# OAUTH PROVIDERS (Optional)
# ===========================================
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ===========================================
# AI CLASSIFICATION (Optional)
# ===========================================
# OpenAI API key for story classification
# If not set, stories are auto-approved
OPENAI_API_KEY=""

# ===========================================
# WEB3 (Optional)
# ===========================================
# WalletConnect project ID for wallet connection
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=""

# ===========================================
# ENVIRONMENT
# ===========================================
NODE_ENV="development"
